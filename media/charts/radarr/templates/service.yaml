{{- if .Values.apps.radarr.enabled | default false }}
apiVersion: v1
kind: Service
metadata:
  name: radarr-{{ .Values.instanceName }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: radarr-{{ .Values.instanceName }}
  ports:
    - port: 7878
      targetPort: 7878
{{- end }}
